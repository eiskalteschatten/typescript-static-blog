<div class="item-tile col-12 col-sm-6 d-flex flex-column gap-4">
  <a href="<%= item.link %>/" data-link <% if (item.openInNewTab) { %>target="_blank"<% } %>>
    <% if (item.image) { %>
      <img src="<%= item.image %>" class="item-tile-image large" alt="<%= item.title %>" <% if (typeof noLazyLoading === 'undefined' || !noLazyLoading) { %>loading="lazy"<% } %>>
    <% } else { %>
      <div class="no-item-tile-title-image large">
        <span class="material-icons">article</span>
      </div>
    <% } %>
  </a>

  <h2 class="item-tile-name">
    <a href="<%= item.link %>/" data-link <% if (item.openInNewTab) { %>target="_blank"<% } %>>
      <%= item.title %>
    </a>
  </h2>

  <div class="item-tile-description low-opacity-text">
    <%- item.description %>
  </div>

  <%- include('./itemTileMetaData.ejs') %>
</div>
